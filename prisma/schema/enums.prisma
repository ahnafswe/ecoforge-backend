enum UserRole {
    MEMBER
    ADMIN
}

enum IdeaStatus {
    DRAFT
    PENDING
    APPROVED
    REJECTED
}

enum VoteType {
    UPVOTE
    DOWNVOTE
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
